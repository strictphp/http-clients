name: Static Analysis

on:
  push:
  pull_request:

jobs:
  ecs:
    name: Code Style
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          # strictphp/conventions requires PHP >= 8.3
          php-version: 8.3
          coverage: none

      - run: composer install --no-progress --prefer-dist
      - run: composer cs

  phpstan:
    name: PHPStan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          # strictphp/conventions requires PHP >= 8.3
          php-version: 8.3
          coverage: none

      - run: composer install --no-progress --prefer-dist
      - run: composer stan -- --no-progress
